import {
  Card,
  CardAction,
  CardContent,
  CardDescription,
  CardFooter,
  CardHeader,
  CardTitle,
} from "@/components/ui/card"
import { Car } from "lucide-react"

export default function makecard(text: string) {
    return (
    <Card>
        <CardContent>text</CardContent>
    </Card>
)
};